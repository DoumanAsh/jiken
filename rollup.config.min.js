import base from './rollup.config.js';
import uglify from 'rollup-plugin-uglify';

base.plugins.push(uglify());
base.targets = [
    { dest: 'dist/jiken.cjs.min.js', format: 'cjs' },
    { dest: 'dist/jiken.min.js', format: 'umd' },
];

export default base
